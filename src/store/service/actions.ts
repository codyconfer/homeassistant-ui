import { ActionTree } from 'vuex';
import axios from 'axios';
import { AppState } from '../types';
import { ServiceState } from './types';

export const actions: ActionTree<ServiceState, AppState> = {};
